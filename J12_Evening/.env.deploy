export PROJECT_ID="items-api-470912"
export PROJECT_NUMBER="$(gcloud projects describe "${PROJECT_ID}" --format='value(projectNumber)')"
export REGION="europe-west1"
export REPO="items-repo"
export IMAGE="items-api"
export TAG="v1"
export SERVICE="items-api"
export IMAGE_URI="${REGION}-docker.pkg.dev/${PROJECT_ID}/${REPO}/${IMAGE}:${TAG}"
export GITHUB_REPO="Anithyan/ai-engineer-roadmap"   # ← ton repo (org/repo), forme EXACTE  # ajouté au J14_PM
export DEPLOYER_SA="gh-deployer@${PROJECT_ID}.iam.gserviceaccount.com" # ajouté au J14_PM
export RUNTIME_SA="${PROJECT_NUMBER}-compute@developer.gserviceaccount.com" # ajouté au J14_PM
